# Copia este archivo a ".env" y ajusta los valores antes de levantar los contenedores.
# El archivo ".env" NO se debe commitear (ya esta en .gitignore).

DB_NAME=mercado_db
DB_USER=mercado_user
DB_PASSWORD=admin
DB_ROOT_PASSWORD=root
DB_PORT=3306

BACKEND_PORT=8080
